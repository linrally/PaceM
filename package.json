{
  "name": "PaceM",
  "private": true,
  "packageManager": "yarn@3.6.4",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "backend": "docker-compose up backend",
    "dev": "docker-compose run --rm --service-ports --entrypoint sh dev"
  }
}
